extends layout.pug

block content
  p.hero
    strong FlyGit
    |  serves raw files directly from GitHub<br>with&nbsp;proper&nbsp;<strong>Content-Type</strong>&nbsp;headers

  .url-helper
    .url-paste
      p#url-form
        label.offscreen(for="url") GitHub URL:
        //- value="{{url}}"
        input#url.url(type="url" placeholder="Paste a GitHub file or gist URL here." autofocus tabindex="1")

    .column.left
      h2
        | Use this URL in
        strong
          |  production

      p.url-container
        - url = "https://" + cdnDomain + "/user/repo/tag/file"
        input#url-prod.url(placeholder=url readonly tabindex="2")
        button#url-prod-copy.url-copy-button(data-clipboard-target="#url-prod" title="Copy URL" disabled)
          img(src="/img/clippy.svg" alt="")

      ul
        li No traffic limits or throttling. Files are served via
          |
          a(href="https://fly.io/") Fly.io
          | 's super fast global CDN.
        li Use a specific tag or commit hash in the URL (not a branch). Files are cached permanently based on the URL. Query strings are ignored.
        li The catch: this is a free service, so there are
          a(href="https://github.com/rgrove/rawgit/blob/master/FAQ.md#i-need-guaranteed-100-uptime-should-i-use-cdnrawgitcom")  no uptime or support guarantees.

    .column.right
      h2
        | Use this URL in
        strong
          |  development

      p.url-container
        - url = "https://" + devDomain + "/user/repo/tag/file"
        input#url-dev.url(placeholder=url readonly tabindex="2")
        button#url-dev-copy.url-copy-button(data-clipboard-target="#url-dev" title="Copy URL" disabled)
          img(src="/img/clippy.svg" alt="")

      ul
        li
          p New changes you push to GitHub will be reflected within minutes.


